// const imgToShipMapper = require("./image_to_ship_mapper.js");

// TODO: Map to new ship objects and check they are of type ship and are the correct ship object
// test("Type to url conversion works", () => {
//   expect(imgToShipMapper.getPicFromType("hi")).toBe("hi.jpg");
// });

// test("Url to type conversion works", () => {
//   expect(imgToShipMapper.getTypeFromPic("hi.jpg")).toBe("hi");
// });

test("", () => expect(3).toBe(3));
